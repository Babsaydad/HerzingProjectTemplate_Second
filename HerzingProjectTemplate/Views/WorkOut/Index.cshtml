@model IEnumerable<HerzingProjectTemplate.Models.WorkOut>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WorkOutType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkoutTypeMode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExerciseList)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Day)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkOutActivityDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkOutType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkoutTypeMode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExerciseList)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Day)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkOutActivityDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkoutId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkoutId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkoutId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
